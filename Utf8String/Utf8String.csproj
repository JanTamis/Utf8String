<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>Utf8String</Title>
        <Authors>Jan Tamis Kossen</Authors>
        <Copyright>© Jan Tamis Kossen</Copyright>
        <PackageTags>Utf8 UTF-8 String</PackageTags>
        <PackageVersion>0.1.2</PackageVersion>
        <PackageId>Utf8-String</PackageId>
        <TargetFrameworks>net6.0;net7.0</TargetFrameworks>
        <LangVersion>preview</LangVersion>
        <PackageLicenseUrl></PackageLicenseUrl>
        <PackageReleaseNotes>Added Parsing and String interpolation</PackageReleaseNotes>
        <Description>A implementation of the string class but using the UTF-8 encoding</Description>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <DebugSymbols>true</DebugSymbols>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <DebugType>full</DebugType>
    </PropertyGroup>

</Project>
